BBDD:
- Ejecutar el script ddl, ase

BACKEND:
- Desde build/backend/: 
	java -jar xxxx.jar

FRONTEND:
- Desde build/frontend/:
	(solo es necesario la primera vez)
	npm install -g serve 
	serve -d -C -S


REQUISITOS:
Java >17
node > 14